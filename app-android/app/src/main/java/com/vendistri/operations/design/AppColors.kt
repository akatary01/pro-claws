package com.vendistri.operations.design

import androidx.compose.ui.graphics.Color

object AppColors {
    val vendBlue = Color(0xFF0D61E0)
    val statusUnassigned = vendBlue
    val chipBackground = Color(0xFFF2F2F7)
    val background = Color(0xFFF2F2F7)
    val surface = Color.White
    val border = Color(0xFFD1D1D6)
    val muted = Color(0xFF6E6E73)
    val statusDone = Color(0xFF33CC73)
    val statusPending = Color(0xFFF5B033)
    val statusError = Color(0xFFF04545)

    val done = statusDone
    val pending = statusPending
    val error = statusError
}
